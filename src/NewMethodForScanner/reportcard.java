package NewMethodForScanner;

import java.util.*;

/*public class RtCard {

    String studentName;
    int rollNo;
    int[] marks;

    public RtCard(String studentName, int rollNo, int[] marks) {
        this.studentName = studentName;
        this.rollNo = rollNo;
        this.marks = marks;
    }

    public int calculateTotal() {
        int total = 0;
        for (int m : marks) total += m;
        return total;
    }

    public double calculatePercentage() {
        return calculateTotal() / (double) marks.length;
    }

    public char calculateGrade() {
        double percent = calculatePercentage();
        if (percent >= 90) return 'A';
        if (percent >= 75) return 'B';
        if (percent >= 60) return 'C';
        return 'D';
    }

    public void saveToDatabase() {
        System.out.println("Saving report card to database...");
    }

    public void printReport() {
        System.out.println("Name: " + studentName);
        System.out.println("Roll No: " + rollNo);
        System.out.println("Percentage: " + calculatePercentage());
        System.out.println("Grade: " + calculateGrade());
    }

    // 🔑 ENTRY POINT — THIS WAS MISSING
    public static void main(String[] args) {

        int[] marks = {85, 90, 78};
        ReportCard rc = new ReportCard("Adi", 101, marks);

        rc.saveToDatabase();
        rc.printReport();
    }
}*/

